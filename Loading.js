import React from "react";
import {StyleSheet, Text, View} from "react-native";

function Loading() {
    return <View> </View>
}

const styles = StyleSheet.create({
    container: {
        flex:1;
        
    }
})